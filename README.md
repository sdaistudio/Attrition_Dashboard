# Employee attrition dashboard

Interactive dashboard on the IBM HR Employee Attrition sample dataset (1,470 employees).
Single self-contained HTML file — data embedded, charts via Chart.js CDN.

Live: https://YOUR-USERNAME.github.io/attrition-dashboard/

To update: replace `index.html` and push.
